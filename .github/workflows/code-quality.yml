name: Code Quality

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: code-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  flutter-quality:
    uses: ElevenID/.github/.github/workflows/quality-flutter.yml@9b6141e294da5c0f9254cfaa60c8934df95ec68b
    with:
      flutter-version: "3.44.6"
      test-command: flutter test --coverage
      build-command: flutter build apk --debug --flavor netknights -t lib/main_document.dart
