name: Open-source policy

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ElevenID/.github/.github/actions/open-source-audit@8e3c9bac4b2aa7b1c49494cbf950c53b4ff55f3a

  dependency-review:
    if: github.event_name == 'pull_request' && github.event.repository.private == false
    uses: ElevenID/.github/.github/workflows/dependency-review.yml@e72a8018521dae62af30515d9a45a8d7653acc28
    permissions:
      contents: read
